registry=http://localhost:4873/
always-auth=true
@quojs/:registry=http://localhost:4873/
# token is provided at runtime via env var (NEVER check literal tokens into git)
# see: rush .npmrc-publish docs
//localhost:4873/:_authToken=${NPM_AUTH_TOKEN}
